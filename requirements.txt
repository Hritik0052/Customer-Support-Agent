# Pinned to the versions verified on Python 3.14.6.
# Install:  pip install -r requirements.txt

# --- Direct ---
Django==6.0.7
python-decouple==3.8      # reads .env
httpx==0.28.1             # OpenRouter HTTP client
django-htmx==1.28.0       # request.htmx flag for partial responses
pillow==12.3.0            # ImageField, for profile avatars

# --- Transitive (listed for reproducible installs) ---
anyio==4.14.2
asgiref==3.12.1
certifi==2026.6.17
h11==0.16.0
httpcore==1.0.9
idna==3.18
sqlparse==0.5.5
tzdata==2026.3
